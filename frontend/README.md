# UFAZ Slay Meter — Frontend (Vite + React)

## Quick start
1. Create `.env` in the project root and set:
   ```
   VITE_API_URL=http://localhost:5000
   ```
2. Install & run
   ```bash
   npm install
   npm run dev
   ```
3. Open http://localhost:5173

## API routes used
- POST /api/auth/register
- POST /api/auth/login
- GET  /api/auth/me
- POST /api/auth/logout
